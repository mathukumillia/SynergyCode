'use strict';

// 	This commented portion can be used later when files are read from the database instead of the file system
// /*
// * Module dependencies
// */
// var mongoose = require('mongoose'),
// 	editableFile = mongoose.model('editableFile');	

// /**
// * Load a file by its id
// **/
// exports.loadFile = function(req, res, next, id) {
//     editableFile.load(id, function(err, editableFile) {
//         if (err) return next(err);
//         if (!editableFile) return next(new Error('Failed to load file ' + id));
//         req.editableFile = editableFile;
//         next();
//     });
// };

